/**
 * ============================================================================
 * The landing page. Intentionally empty.
 * ============================================================================
 *
 * This is the one file every project REPLACES. The boilerplate ships a blank
 * canvas rather than a hero section nobody wants, because deleting somebody
 * else's marketing copy is more work than writing your own.
 *
 * Everything around it already works and does not need touching: the header
 * with its auth button, the theme, the sign-in and sign-up flows, and the
 * protected area under /dashboard.
 */
export default function LandingPage() {
  return (
    <div className="mx-auto flex min-h-[calc(100dvh-3.5rem)] max-w-5xl items-center px-4">
      {/* Replace everything inside this section. */}
      <section className="w-full py-24" />
    </div>
  );
}
